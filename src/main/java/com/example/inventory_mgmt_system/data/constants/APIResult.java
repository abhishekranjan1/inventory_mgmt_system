package com.example.inventory_mgmt_system.data.constants;

public enum APIResult {
        TOKEN_EXPIRED,
        USER_BLOCKED,
        USER_NOT_ALLOWED,
        INVALID_VALUES,
        USER_NOT_FOUND,
        EXCEPTION,
        UNKNOWN_EXCEPTION,
        PAGE_NUMBER_ERROR,
        INVALID_REQUEST,
        IO_EXCEPTION,
        OK,
        REDIRECT,
        PERMANENT_REDIRECT
}
